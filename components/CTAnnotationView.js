// MapView.js

import { requireNativeComponent } from 'react-native';

// requireNativeComponent automatically resolves this to "CTAnnotationViewManager"
module.exports = requireNativeComponent('CTAnnotationView', null);